# AGENTS.md

## 项目约定

1. 凡是需要构建时，更新 `pom.xml` 和 `plugin.yml` 中的版本号，使其 +1，并确保两处版本完全一致。
2. 涉及插件产物更新时，先清空插件目录中的 `PLUGIN` 内容，再仅复制本次改动对应的产物到 `PLUGIN` 目录。
3. 所有面向用户的外显文本必须使用中文，包括提示信息、日志、报错、命令反馈、界面文本和配置说明。
4. 所有文件操作默认使用 UTF-8 编码；新增或修改文件时，必须确保文件以 UTF-8 保存。
5. 搜索文件名、文件内容或代码文本时，强制使用 `C:\ripgrep\rg.exe`，不得直接调用 `rg`。
6. 执行 ripgrep 时，命令格式必须为 `& 'C:\ripgrep\rg.exe' <参数>`。

## 维护补充

- 不要提交真实登录态、日志、失败截图、私信页面快照或其他隐私数据。
- 不要提交 `target`、`build`、展开后的 `PLUGIN` 目录或 IDE 本地配置。
- 只修改文档且不构建时，不需要递增版本号。
- 调整自动化逻辑时，优先小步修复并保留当前稳定发送策略。