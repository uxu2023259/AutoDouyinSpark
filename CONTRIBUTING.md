# 贡献说明

感谢你愿意改进抖音自动续火花助手。提交前请先阅读以下约定。

## 开发约定

- 所有新增或修改文件必须使用 UTF-8 保存。
- 所有面向用户的外显文本必须使用中文，包括提示、日志、报错、命令反馈、界面文本和配置说明。
- 调整自动化逻辑时，优先小步修改并保留当前稳定发送策略。
- 不要提交真实登录态、日志、截图、私信页面快照或其他敏感数据。
- 不要提交 `target`、`build`、展开后的 `PLUGIN` 目录或本地 IDE 配置。

## 本地验证

```powershell
mvn -DskipTests=false test
```

如需生成发布包：

```powershell
pwsh -ExecutionPolicy Bypass -File .\build.ps1
```

构建会自动递增版本号并同步多个版本文件。如果只是修改文档且不构建，不需要递增版本号。

## 提交信息建议

提交信息建议使用简短中文，例如：

- `修复会话选择器兼容性`
- `补充 GitHub 发布文档`
- `优化失败截图说明`

## 拉取请求检查清单

提交拉取请求前请确认：

- [ ] 已执行必要测试。
- [ ] 用户可见文本均为中文。
- [ ] 新增或修改文件为 UTF-8。
- [ ] 没有提交构建输出、登录态、日志、截图或隐私数据。
- [ ] 如涉及构建，版本号已按规则递增且保持一致。